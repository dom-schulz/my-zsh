# ms CLI

Multi-service workspace management tool.

## Installation

```bash
# From workspace root
pip install -e ./my-zsh
```

## Usage

- **Setup**: `ms setup`
- **List Env**: `ms <repo_alias> env --ls`
- **Modify Env**: `ms <repo_alias> env --m <KEY>`
